NOTE IMPORTANTE : aucune page html n'est renvoyé après chaque méthodes du controleur.
Il faudrait utiliser    this.transitionToRoute('X');     apparemment, ou X est le nom de la route menant à l'ihm. ne pas oublier d'envoyer egalement les variables avec (notamment le projet je pense).



CreationProjet()
	-Requiert un champs "newName"
	-créer un nouveau projet, le save et ? le donne à l'application ?

sauvegarderProjet()
	-sauve le projet

ouvrirProjet()
	-ouvre le projet "projet" choisi

supprimerProjet()
	-supprime le projet "projet" choisi

creationEntite()
	-crée une entite avec les champs "newName", "newType", "newAttributs", "newMethodes"
	-Ajoute l'entite a la liste d entite du projet de l instance

ajouterEntite(ddc, entit)
	-ajoute l'entité "entit" a la liste d'entité du diagramme de classe "ddc"

supprimerEntite(entit)
	-supprime l entite en creant une nouvelle liste sans l'entite "entit", et en ecrasant la listeEntite du projet avec la nouvelle.

creationDC()
	-créé le diagramme de classe
creationDO()
	-créé le diagramme d'objet
creationDS()
	-créé le diagramme de séquence

printtDiagramme()
	-retourne la liste des diagrammes

suppDiagramme
	-supprime un diagramme ??


